//! End-to-end scenarios for Shielded CSV, exercised as integration tests
//! (`tests/`) against a real regtest node with real full-parameter proofs. The
//! flagship is the Tether scenario; see `spec/00-OVERVIEW.md`.
//!
//! The crate body is intentionally empty — everything lives in `tests/` and is
//! added in M8.

// Scenarios land in M8.
