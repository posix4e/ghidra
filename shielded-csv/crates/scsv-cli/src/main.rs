//! `scsv` — command-line driver for Shielded CSV wallets, issuance, auditing,
//! Signal transport, and the end-to-end regtest demos.

fn main() {
    // Subcommands are implemented from M7 onward.
    println!("scsv {}: CLI arrives in M7", env!("CARGO_PKG_VERSION"));
}
